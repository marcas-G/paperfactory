/* AUTO-GENERATED by packages/client/script/generate.ts — do not edit.
 * 契约源：packages/protocol/src/endpoints.ts
 * 重新生成：npx tsx packages/client/script/generate.ts
 */

import type { ClientAdapter } from "../adapter";
import type { AgentRunRequest, AgentRunResponse, ComparePhasesQuery, CreateProjectRequest, CreateProjectResponse, CreateQuestionRequest, DeleteProjectResponse, DeleteResearchObjectResponse, DownloadPdfResponse, EventsQuery, EvidenceChainLink, HealthResponse, Paper, PhaseDecisionRequest, PhaseRunDTO, PhaseRunRecord, PhaseRunsGrouped, ProjectBundle, ProjectDetail, ProjectSummary, ResearchObject, ResearchObjectList, ResearchQuestionRecord, ResearchStreamQuery, ResumeResearchRequest, ResumeResearchResponse, RunPhaseRequest, RunPhaseResponse, RunStatusResponse, StartResearchRequest, StartResearchResponse, StopRunResponse, UpdateProjectRequest, UpdateProjectResponse, UpdateResearchObjectRequest, VersionCompare } from "../../../protocol/src";

export const SDK_GENERATED_ENDPOINT_COUNT = 34;

/** 拼 query string（跳过 undefined/null 值） */
function buildQuery(query?: object): string {
  if (!query) return "";
  const parts: string[] = [];
  for (const [key, value] of Object.entries(query)) {
    if (value === undefined || value === null) continue;
    parts.push(encodeURIComponent(key) + "=" + encodeURIComponent(String(value)));
  }
  return parts.length ? "?" + parts.join("&") : "";
}

/**
 * GET /health
 * 健康检查
 */
export function getHealth(client: ClientAdapter): Promise<HealthResponse> {
  return client.fetch(`/health`) as Promise<HealthResponse>;
}

/**
 * GET /api/events
 * 统一事件流（SSE）；请用 subscribeEvents 订阅，本函数仅用于一次性拉取
 * @sse 响应为 text/event-stream——普通 fetch 将挂到流结束；事件订阅请用 subscribeEvents。
 */
export function getEvents(client: ClientAdapter, query?: EventsQuery): Promise<unknown> {
  return client.fetch(`/api/events${buildQuery(query)}`) as Promise<unknown>;
}

/**
 * POST /api/projects
 * 创建项目（201）
 */
export function createProject(client: ClientAdapter, body: CreateProjectRequest): Promise<CreateProjectResponse> {
  return client.fetch(`/api/projects`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body),
  }) as Promise<CreateProjectResponse>;
}

/**
 * GET /api/projects
 * 项目列表
 */
export function getProjects(client: ClientAdapter): Promise<ProjectSummary[]> {
  return client.fetch(`/api/projects`) as Promise<ProjectSummary[]>;
}

/**
 * GET /api/projects/:id
 * 项目详情（404 = 不存在）
 */
export function getProject(client: ClientAdapter, id: string): Promise<ProjectDetail> {
  return client.fetch(`/api/projects/${encodeURIComponent(id)}`) as Promise<ProjectDetail>;
}

/**
 * PUT /api/projects/:id
 * 更新项目
 */
export function updateProject(client: ClientAdapter, id: string, body: UpdateProjectRequest): Promise<UpdateProjectResponse> {
  return client.fetch(`/api/projects/${encodeURIComponent(id)}`, {
    method: "PUT",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body),
  }) as Promise<UpdateProjectResponse>;
}

/**
 * DELETE /api/projects/:id
 * 删除项目（级联删下属对象）
 */
export function deleteProject(client: ClientAdapter, id: string): Promise<DeleteProjectResponse> {
  return client.fetch(`/api/projects/${encodeURIComponent(id)}`, { method: "DELETE" }) as Promise<DeleteProjectResponse>;
}

/**
 * GET /api/projects/:id/hypotheses
 * 项目假设列表
 */
export function getProjectHypotheses(client: ClientAdapter, id: string): Promise<ResearchObjectList> {
  return client.fetch(`/api/projects/${encodeURIComponent(id)}/hypotheses`) as Promise<ResearchObjectList>;
}

/**
 * GET /api/projects/:id/evidence
 * 项目证据列表
 */
export function getProjectEvidence(client: ClientAdapter, id: string): Promise<ResearchObjectList> {
  return client.fetch(`/api/projects/${encodeURIComponent(id)}/evidence`) as Promise<ResearchObjectList>;
}

/**
 * GET /api/projects/:id/knowledge
 * 项目文献知识项列表
 */
export function getProjectKnowledge(client: ClientAdapter, id: string): Promise<ResearchObjectList> {
  return client.fetch(`/api/projects/${encodeURIComponent(id)}/knowledge`) as Promise<ResearchObjectList>;
}

/**
 * GET /api/projects/:id/reports
 * 项目报告列表
 */
export function getProjectReports(client: ClientAdapter, id: string): Promise<ResearchObjectList> {
  return client.fetch(`/api/projects/${encodeURIComponent(id)}/reports`) as Promise<ResearchObjectList>;
}

/**
 * GET /api/projects/:id/all
 * 项目全部研究对象打包
 */
export function getProjectAll(client: ClientAdapter, id: string): Promise<ProjectBundle> {
  return client.fetch(`/api/projects/${encodeURIComponent(id)}/all`) as Promise<ProjectBundle>;
}

/**
 * GET /api/projects/:id/papers
 * 项目文献列表（按时间倒序）
 */
export function getProjectPapers(client: ClientAdapter, id: string): Promise<Paper[]> {
  return client.fetch(`/api/projects/${encodeURIComponent(id)}/papers`) as Promise<Paper[]>;
}

/**
 * GET /api/projects/:id/phases
 * 阶段运行记录
 */
export function getProjectPhases(client: ClientAdapter, id: string): Promise<PhaseRunDTO[]> {
  return client.fetch(`/api/projects/${encodeURIComponent(id)}/phases`) as Promise<PhaseRunDTO[]>;
}

/**
 * GET /api/projects/:id/phases/grouped
 * 阶段运行记录（按 phaseName 分组）
 */
export function getProjectPhasesGrouped(client: ClientAdapter, id: string): Promise<PhaseRunsGrouped> {
  return client.fetch(`/api/projects/${encodeURIComponent(id)}/phases/grouped`) as Promise<PhaseRunsGrouped>;
}

/**
 * GET /api/projects/:id/phases/:phaseName/versions
 * 指定阶段的全部版本
 */
export function getPhaseVersions(client: ClientAdapter, id: string, phaseName: string): Promise<PhaseRunDTO[]> {
  return client.fetch(`/api/projects/${encodeURIComponent(id)}/phases/${encodeURIComponent(phaseName)}/versions`) as Promise<PhaseRunDTO[]>;
}

/**
 * GET /api/projects/:id/phases/:phaseName/compare
 * 阶段版本对比（含行级 diff）
 */
export function comparePhaseVersions(client: ClientAdapter, id: string, phaseName: string, query?: ComparePhasesQuery): Promise<VersionCompare> {
  return client.fetch(`/api/projects/${encodeURIComponent(id)}/phases/${encodeURIComponent(phaseName)}/compare${buildQuery(query)}`) as Promise<VersionCompare>;
}

/**
 * GET /api/projects/:id/chain/:objectType/:objectId
 * 证据链上下游
 */
export function getEvidenceChain(client: ClientAdapter, id: string, objectType: string, objectId: string): Promise<EvidenceChainLink> {
  return client.fetch(`/api/projects/${encodeURIComponent(id)}/chain/${encodeURIComponent(objectType)}/${encodeURIComponent(objectId)}`) as Promise<EvidenceChainLink>;
}

/**
 * POST /api/projects/:id/phases/:runId/decision
 * 人工审批（manual 模式：approve | modify | reject）
 */
export function submitPhaseDecision(client: ClientAdapter, id: string, runId: string, body: PhaseDecisionRequest): Promise<PhaseRunRecord> {
  return client.fetch(`/api/projects/${encodeURIComponent(id)}/phases/${encodeURIComponent(runId)}/decision`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body),
  }) as Promise<PhaseRunRecord>;
}

/**
 * POST /api/projects/:id/phases/:phaseName/run
 * 单阶段重跑（未知 phase 返回 404）
 */
export function runPhase(client: ClientAdapter, id: string, phaseName: string, body?: RunPhaseRequest): Promise<RunPhaseResponse> {
  return client.fetch(`/api/projects/${encodeURIComponent(id)}/phases/${encodeURIComponent(phaseName)}/run`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body ?? {}),
  }) as Promise<RunPhaseResponse>;
}

/**
 * GET /api/papers/:citationId
 * 单篇文献详情（404 = 不存在）
 */
export function getPaper(client: ClientAdapter, citationId: string): Promise<Paper> {
  return client.fetch(`/api/papers/${encodeURIComponent(citationId)}`) as Promise<Paper>;
}

/**
 * POST /api/papers/:citationId/download-pdf
 * 登记 PDF 下载（返回本地路径）
 */
export function downloadPaperPdf(client: ClientAdapter, citationId: string): Promise<DownloadPdfResponse> {
  return client.fetch(`/api/papers/${encodeURIComponent(citationId)}/download-pdf`, { method: "POST" }) as Promise<DownloadPdfResponse>;
}

/**
 * POST /api/research/run
 * 发起研究（202 秒回 runId；全部进度经 /api/events 广播）
 */
export function startResearch(client: ClientAdapter, body: StartResearchRequest): Promise<StartResearchResponse> {
  return client.fetch(`/api/research/run`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body),
  }) as Promise<StartResearchResponse>;
}

/**
 * POST /api/research/resume
 * 断点续跑（202；依据 PhaseRun 跳过已完成阶段，从下一阶段继续；404 = 项目不存在）
 */
export function resumeResearch(client: ClientAdapter, body: ResumeResearchRequest): Promise<ResumeResearchResponse> {
  return client.fetch(`/api/research/resume`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body),
  }) as Promise<ResumeResearchResponse>;
}

/**
 * POST /api/research/stream
 * 一次性研究流（legacy 直连 SSE；多端推荐 run + subscribeEvents 组合）
 * @sse 响应为 text/event-stream——普通 fetch 将挂到流结束；事件订阅请用 subscribeEvents。
 */
export function startResearchStream(client: ClientAdapter, body: StartResearchRequest): Promise<unknown> {
  return client.fetch(`/api/research/stream`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body),
  }) as Promise<unknown>;
}

/**
 * GET /api/research/stream
 * 一次性研究流（GET + q 参数；legacy）
 * @sse 响应为 text/event-stream——普通 fetch 将挂到流结束；事件订阅请用 subscribeEvents。
 */
export function getResearchStream(client: ClientAdapter, query: ResearchStreamQuery): Promise<unknown> {
  return client.fetch(`/api/research/stream${buildQuery(query)}`) as Promise<unknown>;
}

/**
 * POST /api/research/:runId/stop
 * 停止运行（404 = run 不存在/已结束）
 */
export function stopResearchRun(client: ClientAdapter, runId: string): Promise<StopRunResponse> {
  return client.fetch(`/api/research/${encodeURIComponent(runId)}/stop`, { method: "POST" }) as Promise<StopRunResponse>;
}

/**
 * GET /api/research/:runId/status
 * 运行状态（404 = run 不存在/已结束）
 */
export function getResearchRunStatus(client: ClientAdapter, runId: string): Promise<RunStatusResponse> {
  return client.fetch(`/api/research/${encodeURIComponent(runId)}/status`) as Promise<RunStatusResponse>;
}

/**
 * POST /api/research/questions
 * 创建研究问题（201）
 */
export function createResearchQuestion(client: ClientAdapter, body: CreateQuestionRequest): Promise<ResearchQuestionRecord> {
  return client.fetch(`/api/research/questions`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body),
  }) as Promise<ResearchQuestionRecord>;
}

/**
 * GET /api/research/:objectId
 * 按 id 查研究对象（跨 12 种类型；404 = 不存在）
 */
export function getResearchObject(client: ClientAdapter, objectId: string): Promise<ResearchObject> {
  return client.fetch(`/api/research/${encodeURIComponent(objectId)}`) as Promise<ResearchObject>;
}

/**
 * PUT /api/research/:objectId
 * 更新研究对象（404 = 不存在）
 */
export function updateResearchObject(client: ClientAdapter, objectId: string, body: UpdateResearchObjectRequest): Promise<ResearchObject> {
  return client.fetch(`/api/research/${encodeURIComponent(objectId)}`, {
    method: "PUT",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body),
  }) as Promise<ResearchObject>;
}

/**
 * DELETE /api/research/:objectId
 * 删除研究对象（404 = 不存在）
 */
export function deleteResearchObject(client: ClientAdapter, objectId: string): Promise<DeleteResearchObjectResponse> {
  return client.fetch(`/api/research/${encodeURIComponent(objectId)}`, { method: "DELETE" }) as Promise<DeleteResearchObjectResponse>;
}

/**
 * POST /api/agent/run
 * 一次性 agent 循环（同步等待结果）
 */
export function runAgent(client: ClientAdapter, body: AgentRunRequest): Promise<AgentRunResponse> {
  return client.fetch(`/api/agent/run`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body),
  }) as Promise<AgentRunResponse>;
}

/**
 * POST /api/agent/stream
 * agent 循环流式返回（SSE）
 * @sse 响应为 text/event-stream——普通 fetch 将挂到流结束；事件订阅请用 subscribeEvents。
 */
export function streamAgent(client: ClientAdapter, body: AgentRunRequest): Promise<unknown> {
  return client.fetch(`/api/agent/stream`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body),
  }) as Promise<unknown>;
}
