"""Evals (skeleton).

Will own controller eval, cognitive eval, agent eval, workflow eval,
long-horizon eval, and fixtures. Business unit tests still live under the
top-level tests/ directory. No eval logic is implemented yet (STEP-001 only).
"""
