"""M6 — Data / Persistence Plane (skeleton).

Will own repositories, PostgreSQL adapters, domain event persistence, typed
relation persistence, vector index adapters, and artifact metadata. Will
implement ports defined by the Domain layer; must not define Domain
semantics. No business logic is implemented yet (STEP-001 only).
"""
