"""M3 — Agent Runtime (skeleton).

Will own Session, Run, agent/skill/tool execution, subagent isolation,
retry, timeout, checkpoint, resume, permission runtime, sandbox runtime,
and hooks. No business logic is implemented yet (STEP-001 only).
"""
