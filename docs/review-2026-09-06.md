# 全面审阅报告 — PaperFactory

**日期:** 2026-09-06
**范围:** 全量代码、测试、架构一致性、Docker 构建

---

## 一、需修复的问题

### CRITICAL（必须修）

| # | 文件 | 问题 | 影响 |
|---|------|------|------|
| C1 | `loop.ts:63-157` | `AgentEvent` 要求 `timestamp: string`，但所有 `realEmit` 调用省略 `timestamp`（依赖 `emit` 函数行 44 的突变赋值） | 9 个类型错误 |
| C2 | `loop.test.ts:102` | `runAgentLoop(..., 3)` — 第 4 参数应为 `AgentLoopOptions` 对象，传了裸数字 `3` | 类型错误 |
| C3 | `error-handling.test.ts:17,46,81` | 同上：传裸数字 `3`/`2`/`5` 而非 `{ maxIterations: N }` | 类型错误 |
| C4 | `executor.ts:34` | 同上：`this.config.maxIterations ?? 10`（裸数字） | 类型错误 |
| C5 | `modes.test.ts:6` | `expect(COGNITIVE_MODES.length).toBe(10)` — 实际 13 个（加了 PLAN/REFLECT/DEBATE） | 测试失败 |

### HIGH（建议修）

| # | 文件 | 问题 | 影响 |
|---|------|------|------|
| H1 | `phase-contracts.ts:465,479` | `"self:review"` 不在 `AgentEventType` 联合类型中 | 类型错误，前端能处理但 TS 报错 |
| H2 | `routes.ts:559` | `researchResult.hypothesisStatus` — 该字段不存在，返回 `hypothesisStatements` | 前端收到 `undefined` |
| H3 | `hypothesis-verification.ts:55,73` | `Effect.runSync()` 调用异步 `objectStore.save()` | CLI 崩溃（`UnhandledTraversedError`） |

### MEDIUM（建议修）

| # | 文件 | 问题 |
|---|------|------|
| M1 | `pg-object-store.ts:55` | `detectType` 对 `Report` 要求 `sectionIds` 存在，`createReport` 已包含此字段 — 实际 OK |
| M2 | `pg-object-store.ts:59` | `detectType` 对 `Experiment` 要求 `resultIds` 存在，`createExperiment` 已包含 — 实际 OK |
| M3 | `routes.ts:501` | `globalThis.__researchRuns` 全局状态 — 不支持水平扩展 |
| M4 | `phase-contracts.ts:232` | `(item.strength ?? 0).toFixed(2)` — `strength` 类型为 `unknown`，需要类型断言 |
| M5 | `phase-contracts.ts:618` | `{ openAccessPdf: ..., citationCount: ... } ?? {}` — 左边永远非 null，`?? {}` 无效代码 |

---

## 二、本次计划修复的范围

本次修复以下问题（按优先级排序）：

1. **C1**: 修复 `loop.ts` — 所有 `realEmit` 调用添加 `timestamp`
2. **C2, C3, C4**: 修复 `runAgentLoop` 调用 — 裸数字改为 `{ maxIterations: N }`
3. **C5**: 更新 `modes.test.ts` — 10 → 13，补充新模式的断言
4. **H1**: 扩展 `AgentEventType` — 加入 `"self:review"`
5. **H2**: 修复 `routes.ts:559` — `hypothesisStatus` → 正确的字段
6. **H3**: 修复 `hypothesis-verification.ts` — `runSync` → `runPromise`

---

## 三、待明确的问题

（见下方用户确认）
