# PaperFactory 全面问题清单

## P0 — 功能不可用

| # | 问题 | 位置 | 根因 |
|---|------|------|------|
| 1 | **项目删除无级联删除** — 删除项目时 PhaseRun/Citation/EvidenceChain 等关联数据残留 | routes.ts:184-192 | 无 cascade 逻辑，只删 Project 一行 |
| 2 | **GET /api/projects/:id/all 重复定义** — Hono 第一个 handler 返回，第二个不被调用，导致 citations 丢失 | routes.ts:268-282 和 routes.ts:668-684 | 同一路由注册两次 |
| 3 | **前端无删除按钮** — 用户无法从 UI 删除项目 | index.html | renderProjects 无删除入口 |
| 4 | **SSE /api/agent/stream 实现错误** — c.respondWith() 和 return Response 混用 | routes.ts:346-380 | 混淆 Hono Context API 和 Fetch API |
| 5 | **self:review 事件前端未处理** — phase-contracts 发送 self:review，前端忽略 | phase-contracts.ts:404, index.html | AgentEventType 无 self:review，前端无 handler |

## P1 — 数据不完整

| # | 问题 | 位置 | 根因 |
|---|------|------|------|
| 6 | **phases API 排序字段名错误** — 用 phase_version 但对象是 phaseVersion | routes.ts:692 | 混用 snake_case 和 camelCase |
| 7 | **phases API 不返回 objectType/objectId** — 前端 loadPhaseRuns 需要这两个字段 | routes.ts:696-697 | 映射时遗漏 |
| 8 | **report_generation 内容丢失** — 构建了 content 但未保存到 Report 对象 | phase-contracts.ts:589-597 | content 变量被丢弃 |
| 9 | **experiment_execution 沙箱是假实验** — 硬编码 print，不执行 LLM 生成的代码 | phase-contracts.ts:543-546 | Sandbox 存根 |
| 10 | **研究崩溃数据全丢** — 全程 InMemoryObjectStore，崩溃后无持久化 | routes.ts:402-406 | 架构设计 |

## P2 — 体验问题

| # | 问题 | 位置 | 根因 |
|---|------|------|------|
| 11 | **showView 使用全局 event** — 非标准，严格模式可能不支持 | index.html:672 | 未传 event 参数 |
| 12 | **showView 无实际视图切换** — 只切换 active class | index.html:672-675 | 未实现 |
| 13 | **SKIPPED 状态前端显示为 error** — 误导 | index.html:488 | 前端状态映射不完整 |
| 14 | **DELETE /api/research/:objectId 只删 ResearchQuestion** | routes.ts:319-322 | 硬编码类型 |
| 15 | **runId 命名混淆** — 主 runId 和 phaseRunId 在审批流程中混用 | routes.ts:536,572-582 | 命名不规范 |
| 16 | **esc 函数 XSS 风险** — 缺少 ' " / 转义 | index.html:344 | 不完整转义 |

---

## 修复计划

按优先级依次修复：
1. P0-1: 级联删除
2. P0-2: 合并 /all 端点
3. P0-3: 前端加删除按钮
4. P0-6: 排序字段修正
5. P0-7: phases API 返回 objectType/objectId
6. P0-5: self:review 前端处理
7. P2-11: showView 传 event
8. P2-16: esc 函数补全
